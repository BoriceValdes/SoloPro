services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: solopro
    ports:
      - "5432:5432"
    volumes:
      - solopro_data:/var/lib/postgresql/data

volumes:
  solopro_data:
